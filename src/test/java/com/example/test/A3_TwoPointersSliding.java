package com.example.test;

import java.util.*;

public class A3_TwoPointersSliding {

    /*
     * ===========================
     * 3. Two Pointers / Sliding Window
     * ===========================
     */

    // Problem: Sliding Window (fixed length) - Max Sum
    // Description: 길이 k 윈도우 중 합의 최댓값을 반환
    // Input: int[] arr, int k
    // Output: int maxSum
    static int maxSumFixedWindow(int[] arr, int k) {
        // TODO
        return 0;
    }

    // Problem: Sliding Window (variable length) - Min length subarray with sum >=
    // target
    // Description: 합이 target 이상이 되는 부분배열 중 최소 길이를 반환. 없으면 0.
    // Input: int[] arr, int target
    // Output: int minLen
    static int minLenSubarrayAtLeastTarget(int[] arr, int target) {
        // TODO
        return 0;
    }

    // Problem: Two Pointers - Remove Duplicates (sorted array)
    // Description: 정렬된 배열에서 중복 제거 후 길이를 반환 (in-place 스타일)
    // Input: int[] arr
    // Output: int newLen
    static int removeDuplicatesSorted(int[] arr) {
        // TODO
        return 0;
    }

    // Problem: Subarray Sum Equals K
    // Description: 합이 k인 부분배열의 개수를 반환 (prefix + hashmap)
    // Input: int[] arr, int k
    // Output: int count
    static int countSubarraySumEqualsK(int[] arr, int k) {
        // TODO
        return 0;
    }

    // Problem: Longest Substring Without Repeating Characters
    // Description: 문자열 s에서 중복 없는 가장 긴 부분문자열 길이 반환
    // Input: String s
    // Output: int maxLen
    static int longestSubstringNoRepeat(String s) {
        // TODO: sliding window + last index
        return 0;
    }
}
